import styled from "styled-components";
import { white, black2 } from "../variables/colors";

const Title = styled.h2`
  margin-bottom: 2rem;
  font-size: 3rem;
  color: ${white};
  background-color: ${black2};
  padding: 0 30px;
  margin: 0;
`;

export default Title;
